// Generated by CoffeeScript 1.6.3
(function() {
  $(document).ready(function() {
    $('.interestList .search input').keyup(function() {
      var clearSearch, text;
      clearSearch = $('.interestList .search .cancel');
      text = $(this).val();
      if (text.length > 0) {
        return clearSearch.show();
      } else {
        return clearSearch.hide();
      }
    });
    return $(document).on('click', '.interestList .search .cancel', function() {
      var form, searchField;
      form = $('.interestList .search form');
      searchField = $('.interestList .search input');
      $.ajax({
        type: form.attr('method'),
        url: js_url(form.attr('action')),
        success: function(response) {
          return $('.interestList .results').html(response.browse_results);
        }
      });
      searchField.val('');
      searchField.focus();
      $(this).hide();
      return false;
    });
  });

}).call(this);
