// Generated by CoffeeScript 1.6.3
(function() {
  $(document).ready(function() {
    return $(document).on('click', '.pick button', function() {
      var form;
      form = $(this).closest('form');
      $.ajax({
        data: form.serialize(),
        type: form.attr('method'),
        url: form.attr('action'),
        success: function(response) {
          return $.ajax({
            type: 'GET',
            url: '/u/tutorial',
            success: function(response) {
              $('.pick').remove();
              $('.content').html(response.tutorial);
              $('.tutorial #redirect').attr('class', response.redirect);
              return $('.tutorial').fadeIn(100, function() {
                var user;
                $('.tutorial .insideBlocker').height($('.slide1 .inside').height());
                user = $('.tutorial #tutorialUserType').attr('class');
                if (user === 'tutee') {
                  return $('.tutorial header .browse, \
                  .tutorial .search .field input').animate({
                    borderColor: 'rgb(255, 26, 0)'
                  }, 1000);
                } else {
                  return $('.tutorial .slide1 input, \
                  .tutorial .aboutTutorTutorial').animate({
                    borderColor: 'rgb(255, 26, 0)'
                  }, 1000);
                }
              });
            }
          });
        }
      });
      return false;
    });
  });

}).call(this);
